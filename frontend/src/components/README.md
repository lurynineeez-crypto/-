# Component System

原型阶段已形成这些稳定 UI 角色：

- 指标卡：核心环境指标、评分、批次摘要。
- 状态点：正常、预警、报警、离线。
- 表格：点位、维护、审计、配置。
- 时间轴：报警闭环、发布记录、事件过程。
- 标签：数据状态、角色权限、阈值说明。
- 面板：所有模块统一使用 8px 圆角和轻量边框。

后续工程化时可拆成 `MetricCard`、`StatusDot`、`DataTable`、`Timeline`、`PanelBlock`。
